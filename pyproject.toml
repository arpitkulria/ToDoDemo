[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["Arpit Suthar <arpit.kulria@gmail.com>"]
description = ""
name = "tododemo"
version = "0.1.0"
[tool.poetry.dependencies]
django = "^3.0"
python = "^3.7"
[tool.poetry.dev-dependencies]
